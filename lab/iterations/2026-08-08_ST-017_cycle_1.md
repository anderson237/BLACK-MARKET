# ST-017 — Pipeline d'import Xianyu / Goofish — Cycle 1

**Date** : 2026-08-08
**Gate courant** : 1 → 4 (Hypothèse + Web research + Implémentation)
**Statut** : 🚧 En cours — backend + UI déployés ; test e2e admin à réaliser ; solde 1688 à recharger

## Objectif
Permettre à l'utilisateur d'importer **~150 produits/semaine** depuis Xianyu (闲鱼 / Goofish, `https://www.goofish.com/`) et **1688 (gros)** vers le catalogue Deep Roots, avec : images re-téléchargées (pas de hotlink), conversion prix CNY → XOF, traduction/enrichissement Gemini, catégories et publications propres.

## Rappel du problème
Xianyu est une plateforme mobile-first d'Alibaba (C2C, millions d'annonces). L'utilisateur propose 2 options initiales :
- **Option 1** : simulateur + capture du trafic de l'app (Interception TLS)…
- **Option 2** : APK maison installé sur son téléphone, qui écoute le trafic de l'app Xianyu, pour récupérer les données et les pousser vers le site.

Les deux sont **déconseillées**. ⇒ Raisons : violation des CGU Xianyu/Alibaba, détection anti-bot (TLS fingerprinting, captcha), fragilité à chaque mise à jour d'app, risque juridique, et non maintenable à `à cadence 150/semaine`.

## Recherche (gate 3) — état des fait
1. **Pas d'API publique officielle pour LIRE le marketplace Xianyu** : la « Xianyu Open Platform » (淘宝开放平台) n'expose que des API **ISV** réservées aux partenaires (expose : `alibaba.idle.isv.item.publish/edit/query/downshelf`, `alibaba.idle.isv.media.upload`… sur open.fliggy / developer.alibaba.com) — orientées **publication** de produits pour des vendeurs accrédités (准入), pas du « scrape ».
2. **API tierces / scraping managé** pour récupérer données de listing :
   - JustOneAPI : `https://api.justoneapi.com/api/xianyu/search-item-list/v1?token=...&keyword=...` (search) et `/api/xianyu/get-item-detail/v1?itemId=...` (détail).
   - Apify « Goofish Scraper » / « Xianyu Listings Scraper » (per-tél/ per-volume : ~$2/1000 listing, ~$6/1000 détail ; acteur sans login pour feed).
   - Projets open-source (MCP + Playwright + MTOP) : faisable mais maintenance anti-bot.

## Décision utilisateur (2026-08-08)
- **Voie retenue : JustOneAPI** (token fourni `slso0bj9eDR3aBVQ`, valide) — recherche + détail Xianyu & 1688.
- **Pinduoduo : écarté** (absent du catalogue JustOneAPI).
- Scoring fiabilité IA demandé : état (neuf/occasion/défauts), nb avis vendeur, taux réponse, ventes — **couvert** (champs `itemStatusStr`, `remarkDO`, `replyRatio24h`, `hasSoldNumInteger`, `zhimaAuth` validés sur données réelles).

## Implémentation (gate 4) — livré
- `server/utils/justone.ts` : client (search/detail Xianyu + 1688), normalisation, import images → blob `bm-images`, `cnyToXof()` (taux 85 × marge 1.35).
- Routes admin (auth admin) : `POST /api/admin/import/draft` (détail + galerie + draft), `GET /api/admin/import/search`, `POST /api/admin/import/publish` (enrichissement Gemini optionnel + `saveProducts` + `publishSiteUpdate('catalog')`).
- UI `/admin/import` : recherche par plateforme/mot-clé, tri, pagination, aperçu (état, avis vendeur, ventes, Zhima), édition titre/desc/prix/features/catégorie, publication.
- Config : `nuxt.config.ts` runtimeConfig `justone.apiKey` (server-only) + `public.justoneEnabled` ; `.env.example` documenté ; `JUSTONE_API_KEY` posé dans .env local + Netlify production.
- Deploys : `6a7719f076ec248dc93898e1`, `6a771a61f45a1a8b520bfad6`. Commit `60a1add`.

## Blocages / actions
- ⚠️ **Solde JustOne API insuffisant pour `get-item-detail` 1688 (code 601 INSUFFICIENT BALANCE)** — la recherche 1688 et le détail Xianyu passent, mais le détail 1688 est facturé (¥0.10) et le compte n'a plus de crédit. **Action : recharger le compte JustOne (dashboard.justoneapi.com)** avant le test e2e 1688 complet.
- Test e2e admin (UI connectée) : recherche → aperçu → publier à exécuter après recharge.

## Prochaines étapes
1. Recharger le solde JustOne (bloquant pour 1688).
2. Test e2e admin : recherche Xianyu + 1688, draft, enrichissement IA, publication, vérification carte produit live.
3. Itération volume : tests batch (10 produits) puis montée à 150/semaine.