# Handoff retour — Squad 4 → Lab — ST-020 Extension Chrome scraper fournisseurs

- **Date** : 2026-08-10
- **De** : Squad 4 (Dev produit)
- **À** : Lab Director
- **Ticket** : ST-020 / BL-010
- **Statut** : 🚧 **Implémentation faite, NON commitée / NON déployée — à valider par le Lab** (+ test navigateur manuel requis)

## Récapitulatif

L'extension Chrome MV3 est livrée (dossier `browser-extension/`, hors build Nuxt) + la route
serveur complète, réutilisant `buildDraft` sans duplication. Le payload de l'extension peut
contenir du texte asiatique → la **traduction FR Gemini** fonctionne automatiquement
(`hasCjk` → `translateDraftToFrench`), identique au pipeline headless.

## Teste / vérifie

- `npx tsx --tsconfig .nuxt/tsconfig.json scripts/test-extension-import.ts` → **56/56 ✅**
- `npm run build` ✅ (route `extension.post.mjs` dans le bundle Nitro)
- Syntaxe 4 JS extension + manifest JSON ✅
- **À faire par le Lab** : install « Load unpacked » + test sur une vraie page produit
  Taobao (ou `browser-extension/test.html` générique), puis config
  `EXTENSION_IMPORT_KEY` dans l'env **Netlify** avant déploiement.

## Fichiers

| Fichier | Rôle |
|---|---|
| `server/api/admin/import/extension.post.ts` | Route POST — auth x-ext-key/Bearer, validation, `buildDraft` |
| `server/utils/extensionImport.ts` | Logique testable hors Nitro (parse, mapping, auth temps constant) |
| `scripts/test-extension-import.ts` | 3 scénarios / 56 assertions |
| `browser-extension/manifest.json` | MV3 — 2 mondes (MAIN + ISOLATED) host_permissions bornées |
| `browser-extension/content-main.js` | Hook réseau fetch/XHR (world MAIN) |
| `browser-extension/content-isolated.js` | Pool captures + extraction JSON/DOM + DR_PING/DR_CAPTURE |
| `browser-extension/background.js` | Service worker → POST serveur (x-ext-key) |
| `browser-extension/popup.html` + `popup.js` | UI clé + importer + draft |
| `browser-extension/README.md` | Install/config/archi/sécurité |
| `server/utils/draftBuilder.ts` | 1 ligne : `detailSource` + `'extension'` (additif) |
| `.env.example` + README | `EXTENSION_IMPORT_KEY` documentée |

## Points d'attention

1. **Gate lint cassé (préexistant, pas cette itération)** : `npm run lint` → 1447 erreurs sur
   tout le repo (`~~/*` non résolu, globals Nuxt) car `tsconfig.json` racine n'étend pas
   `.nuxt/tsconfig.json`. Suggestion : réparer le tsconfig racine (ajouter
   `"extends": "./.nuxt/tsconfig.json"`) dans un BL séparé pour réactiver le gate.
2. **Prod** : si `EXTENSION_IMPORT_KEY` n'est pas mise dans l'env Netlify, la route renvoie
   **401 volontaire** (pas de fallback dev en prod).
3. V1 = **1 produit par page détail** ; liste de recherche = v2.
4. Backward-compat : `from-url.post.ts`, `publish.post.ts`, UI import **inchangés**.

## Décisions demandées au Lab
- Réparer le gate lint (`tsconfig` racine) → ouvrir BL ?
- Déployer après avoir posé `EXTENSION_IMPORT_KEY` en prod ?
- Itération : `lab/iterations/2026-08-10_ST-020_cycle_1.md`.